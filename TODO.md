- the user should be able to click on a date and manually set a perosn, when
  this person is set it should becme a constrait that his person must take this
  date, the ui should give feedback that this is a manually set date and add a
  reset button , z3 generation should take it into account if the user click
  generate again
